rfdsfqp


git init
git add .
git commit -m "something"
git status
git log --all --graph
git branch name

git checkout name // to change the branch
git checkout master // for main branch

git merge edits // to merger edits in main(master) branch
delete content not required in merge conflict or add something different entirely then add and commit
git merge edits -m "message" // to merger edits in main(master) branch 

git remote add origin URL
git branch -d branch_name //to delete a branch locally
git push origin --delete remoteBranchName // delete brancvh remotely

git branch // to see all branch and current branch
git branch -a  //To see all local and remote branches
git branch -r //To see all local and remote branches
git fetch // to update local files from github